/var/log/nginx/*.log {
    hourly
    size 500M
    maxsize 1000M
    rotate 3
    nocreate
    missingok
    notifempty
    nocompress
    sharedscripts
    postrotate
        /bin/kill -USR1 `cat /run/nginx.pid 2>/dev/null` 2>/dev/null || true
    endscript
}
