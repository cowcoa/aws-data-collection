/var/log/nginx/*.log {
    create 0640 nginx root
    size 10k
    rotate 3
    missingok
    notifempty
    nocompress
    sharedscripts
    postrotate
        /bin/kill -USR1 `cat /run/nginx.pid 2>/dev/null` 2>/dev/null || true
    endscript
}
